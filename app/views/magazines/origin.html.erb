<h1>原作一覧</h1>

<h4><%= @origins.count %></h4>

<% @origins.each do |origin| %>
<p>
  <%= link_to origin.origin,
  controller: 'magazines', action: 'index_origin', id: origin.id, origin: origin.origin %>
</p>
<% end %>
